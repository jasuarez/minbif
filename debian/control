Source: minbif
Section: net
Priority: extra
Maintainer: Romain Bignon <romain@peerfuse.org>
Build-Depends: debhelper (>= 7), cmake,
               imlib11-dev,
               libcaca-dev,
               libpurple-dev
Standards-Version: 3.8.1
Homepage: http://symlink.me/wiki/minbif

Package: minbif
Architecture: any
Depends: ${shlibs:Depends}, ${misc:Depends},
         libcaca0,
         imlib11,
         libpurple0
Description: IRC instant messaging gateway
 It provides these features:
 * Minbif is an IRC server where you connect to.
 * It uses a library which abstracts all IM calls, and has several plugins to support  a lot of IM protocols (IRC included!).
 * Only IRC commands are used to command Minbif.
 * Buddies are IRC users.
 * Each account has a status channel. You see in all connected buddies, and their status on it.
 * Add and remove buddies from list with /INVITE and /KICK commands.
 * Blocked users are bans on the account's status channel.
 * You can see buddies' icons (with libcaca) or download them.
 * Conversation chats are supported.
